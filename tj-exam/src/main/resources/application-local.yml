spring:
  cloud:
    nacos:
      server-addr: 192.168.227.128:8848 # nacos注册中心
      username: nacos
      password: nacos
      discovery:
        namespace: star-lesson-dev
        group: sl-group
        ip: 192.168.29.1
      config:
        namespace: star-lesson-dev
        group: sl-group
        file-extension: yaml
  config:
    import:
      - optional:nacos:${spring.application.name}.yaml # 导入本服务的配置
      - optional:nacos:shared-spring.yaml # 共享spring配置
      - optional:nacos:shared-redis.yaml # 共享redis配置
      - optional:nacos:shared-mybatis.yaml # 共享mybatis配置
      - optional:nacos:shared-logs.yaml # 共享日志配置
      - optional:nacos:shared-feign.yaml # 共享feign配置
      - optional:nacos:shared-mq.yaml # 共享mq配置
      - optional:nacos:shared-xxljob.yaml # 共享mq配置
      - optional:nacos:shared-seata.yaml # 共享seata配置
  # 本地关键配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.227.128:3306/tj_exam?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
  data:
    redis:
      host: 192.168.227.128
      port: 6379
rocketmq:
  name-server: 192.168.227.128:9876
  producer:
    group: exam-service-producer
seata:
  enabled: false
logging:
  level:
    com.tianji: debug
